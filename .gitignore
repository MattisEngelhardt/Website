# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
pnpm-debug.log*

# environment
.env
.env.production

# os
.DS_Store
Thumbs.db

# raw source material from Mattis (huge originals; optimized copies ship via src/ or public/)
# root-only — public/assets/ must ship!
/assets/

# raw pipeline sources: Blender exports, painting scans, PolyHaven downloads
# (regenerable via scripts/ + documented URLs in brain.md; optimized copies ship via public/)
assets-src/

# runtime verification artifacts
verify-out/
